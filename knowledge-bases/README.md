# Knowledge Bases

This directory is intentionally empty by default. Add local Markdown knowledge bases here during development, or configure git-backed sources and destinations with `KNOWLEDGE_SOURCES`, `KNOWLEDGE_DESTINATIONS`, and `KNOWLEDGE_FLOWS`.

