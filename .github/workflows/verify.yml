name: Verify

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    name: Typecheck, test, lint, build
    runs-on: ubuntu-latest

    # Postgres (with pgvector) backs the integration tests for the Postgres*
    # stores. Without DATABASE_URL set, those test blocks self-skip and the SQL
    # paths go uncovered — so the suite runs against a real database here.
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: markdown_magpie
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/markdown_magpie

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Run database migrations
        run: npm run db:migrate

      - name: Test
        run: npm run test

      - name: Lint
        run: npm run lint

      - name: Dead code (knip)
        run: npm run deadcode

      # Builds every workspace in dependency order, including the web app's
      # `next build`. typecheck excludes apps/web (bundler resolution), so this
      # is what catches web/build-only breaks like an unresolvable import.
      - name: Build
        run: npm run build
