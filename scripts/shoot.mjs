// Render the current route-based console UI screenshots used by the deck.
// The renderer uses the app's real CSS plus stable fixture content, so it does
// not require a running API, database, or logged-in production session.
await import("./render-static-ui-shots.mjs");
